import 'package:flutter/material.dart';

class TraderSellCoinView extends StatelessWidget {
  const TraderSellCoinView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
